{
    "server_type": "0",
    "server_name": "rAthena",
    "expected_output": "Config created",
    "validation": {
        "type": "configuration",
        "description": "The server configuration system should correctly handle different server types",
        "requirements": [
            "Configuration should be loadable from JSON",
            "Server type should be correctly identified",
            "Server-specific settings should be applied",
            "Configuration should be validated for required fields",
            "Default values should be applied for missing fields"
        ],
        "security_tests": [
            "Should sanitize user-provided server addresses",
            "Should validate port numbers are in valid range",
            "Should handle malformed configuration gracefully"
        ],
        "compatibility_tests": [
            "Should support all known server types",
            "Should handle unknown server types gracefully",
            "Should be backward compatible with older configuration formats"
        ]
    }
}